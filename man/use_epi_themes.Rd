% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_epi_themes.R
\name{use_epi_themes}
\alias{use_epi_themes}
\title{Add or refresh brand themes in an existing analysis}
\usage{
use_epi_themes(path = ".", which = NULL, overwrite = TRUE)
}
\arguments{
\item{path}{Project directory. Defaults to the working directory.}

\item{which}{Character vector of theme names (without \code{.scss}) to copy, or
\code{NULL} (default) for every theme the package ships.}

\item{overwrite}{Logical. Replace theme files already present? Defaults to
\code{TRUE}, since refreshing is the usual intent.}
}
\value{
The names of the themes written, invisibly.
}
\description{
Copies the bundled brand \code{.scss} theme files out of the installed \code{actepir}
package into an existing project, leaving the analysis document, bibliography
and everything else untouched.
}
\details{
\code{use_epi_analysis()} copies the themes once, when the project is created, so
each project holds a snapshot. Updating \code{actepir} — or pushing a revised
palette to GitHub — has no effect on projects that already exist until this is
run. That snapshot behaviour is deliberate: a report rendered last year keeps
rendering the way it did last year.

Use this to pull in a newly added theme, or to update existing ones after a
palette change. To then apply a refreshed theme, no code is needed — edit the
\verb{theme:} line in the document.
}
\examples{
\dontrun{
# Everything the installed package ships
use_epi_themes()

# Just the current palette
use_epi_themes(which = "acthcsd")
}

}
\seealso{
\code{\link[=use_epi_analysis]{use_epi_analysis()}}
}
\author{
Warren Holroyd
}
\keyword{quarto}
\keyword{template}
