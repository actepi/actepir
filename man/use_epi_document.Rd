% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_epi_document.R
\name{use_epi_document}
\alias{use_epi_document}
\title{Create a single branded Quarto analysis document}
\usage{
use_epi_document(
  name,
  path = ".",
  title = NULL,
  theme = "acthcsd",
  conn = TRUE,
  sel = TRUE,
  sql = FALSE,
  bib = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{name}{Document name, with or without the \code{.qmd} extension. Required.}

\item{path}{Directory to create the document in. Defaults to the working
directory. Created if it does not exist.}

\item{title}{Document title. Defaults to a tidied version of \code{name}.}

\item{theme}{Brand theme, e.g. \code{"acthcsd"} or \code{"acthd"}.}

\item{conn}{Logical. Include the odbc connection chunk for EpiServer 121?}

\item{sel}{Logical. Include the dbplyr selection chunk? Requires \code{conn}.}

\item{sql}{Logical. Include a native SQL chunk? Requires \code{conn}. Off by default.}

\item{bib}{Logical. Include the bibliography stub, worked citations and a
References section? A \code{references.yaml} is written beside the document unless
one is already present.}

\item{overwrite}{Logical. Replace an existing document of the same name?}
}
\value{
Path to the created \code{.qmd}, invisibly.
}
\description{
Writes one branded \code{.qmd} into a folder you already have — inside an existing
project, or anywhere at all — and brings the chosen theme with it. This is the
"New File" counterpart to \code{\link[=use_epi_analysis]{use_epi_analysis()}}, which builds a whole project.
No project folders and no \code{.Rproj} are created.
}
\details{
A Quarto theme is a local file: the document header says \code{theme: acthcsd.scss},
and Quarto resolves that path relative to the document. So a standalone \code{.qmd}
only renders if the \code{.scss} sits beside it. This function copies the chosen
theme next to the new document for exactly that reason; without it, a document
created outside a project would fail to render.

Only the chosen theme is copied, keeping the folder tidy. Switching to a
built-in Bootswatch theme (\code{theme: cosmo}) still needs no file; to bring the
other brand theme in as well, call \code{\link[=use_epi_themes]{use_epi_themes()}}.

An existing \code{.scss} of the same name is never overwritten, so a theme you have
customised in the folder is left untouched.
}
\examples{
\dontrun{
# In the current folder
use_epi_document("subgroup-analysis")

# Somewhere specific, on the legacy blue theme, no database chunk
use_epi_document("adhoc", path = "~/scratch", theme = "acthd", conn = FALSE)
}

}
\seealso{
\code{\link[=use_epi_analysis]{use_epi_analysis()}}, \code{\link[=use_epi_themes]{use_epi_themes()}}
}
\author{
Warren Holroyd
}
\keyword{quarto}
\keyword{template}
